# 変更履歴

本ファイルは、クラッシュロワイヤル対戦分析ツールのバージョンごとの変更内容を記録する。
バージョン番号は [セマンティックバージョニング](https://semver.org/lang/ja/) に従う。

> Git管理は v1.8.0 から開始した。それ以前の日付は、収集ログの記録と
> `docs/requirements.md` の記述から再構成した**おおよその日付**である。
> なお v1.4.0 は欠番で、v1.3.1 から v1.5.0 へ移っている。

---

## [1.8.0] - 2026-09-11

### 変更

- **レベル差と勝率の関係をロジスティック回帰で数式化した**（`src/analysis/levels.py`）

      P(勝ち) = 1 / (1 + exp(-(a + b × レベル差)))

  勝敗は2値・レベル差は連続値なので、この形が素直に対応する。直線で当てはめると
  勝率が 0〜100% の外へ出てしまうため使えない。係数はニュートン法（反復再重み付け
  最小二乗と等価）で求めており、外部ライブラリは使わず標準ライブラリのみで実装している。

  逆ヘッセ行列を係数の分散共分散として扱い、95%信頼区間の帯を描く。帯が広い部分は
  その付近の対戦が少なく、推定が定まっていないことを意味する。当てはまりの良さは
  McFadden の擬似決定係数で示し、実測では 7〜8% 程度だった。つまり**レベル差だけでは
  勝敗の1割も説明できない**ことが数値としてはっきり分かる。

  曲線は実データのある範囲だけを描く。範囲外は根拠が無く外挿になるため。引き分けは
  勝ちとも負けとも言えないため当てはめから除く。試合数が20未満、全勝・全敗、
  レベル差が一定の場合は当てはめが発散するのでモデルを出さない。

- グラフをカテゴリ軸から数値軸へ変更した。実測の点と推定曲線を同じ座標系に載せる
  必要があるため。区分が飛んでいても間隔が正しく空くようになった。

### 削除

- **傾向線を廃止した**。推定曲線が同じ役割（ばらつきをならして傾向を示す）を
  より根拠のある形で果たすため。これに伴い設定値 `level_trend_window` は未使用に
  なったが、設定ファイルの互換のため残している。

### 備考

曲線の傾きは「レベルを上げれば勝率がこれだけ上がる」ことを保証しない。レベルを
上げた時期と上達した時期は重なりやすく、傾きには上達の効果も混ざっている。画面上の
注記にもこの点を明記している。

---

## [1.7.0] - 2026-09-10

### 変更

- レベル分析を棒グラフから折れ線グラフへ変更し、集計の刻みを 0.5 から 0.25 へ
  細かくした。`level_diff` は必ず 0.125 の倍数のため、0.125 が最小の刻みになる。
- 細かくしてもデータ量・処理量は増えない（保存済みの値を振り分けるだけ）。増えるのは
  1区間あたりの試合数が減ることによるばらつきである。そのため実測値とは別に傾向線を
  重ね、前後 `level_trend_window` の範囲の対戦をまとめて区間ごとの跳ねをならした。
- 実測値の点の大きさを対戦数に比例させ、根拠の薄い点が見分けられるようにした。

### 追加

- 刻み幅と傾向線の窓幅を `config.yaml` で調整できるようにした。

---

## [1.6.0] - 2026-09-10

### 変更

- **レベル分析の区分を実データに合わせた**。固定の5段階（-2.0以下／-2.0〜-0.5／
  互角／+0.5〜+2.0／+2.0以上）は実際の分布（-1.25〜+1.9）と合わず、**両端2つが
  常に0件**になっていた。0.5刻みでデータのある区分だけを並べる方式へ変更し、
  空の棒が出ないようにした。
- 棒グラフに全体勝率の破線を重ね、各区分が平均より上か下かを可視化した。
- 各棒の下に対戦数（n）を表示し、根拠の薄い区分が判別できるようにした。

### 追加

- レベル差と勝敗のピアソン相関係数。+1 に近いほどレベル差で勝敗が決まっていることを
  示す。強さの目安も併記する。

---

## [1.5.0] - 2026-09-10

### 追加

- **対戦の詳細画面**（`src/analysis/detail.py`、`/match/<id>`）。タワーの残りHP、
  カード1枚ごとのレベルと進化状態、エリクサー漏れを表示する。対戦履歴の行全体を
  リンクにして遷移する。
- 静的サイトでは絞り込みに依存しない `<secret>/matches/<id>.html` として書き出す。
  組み合わせごとに複製しないことでファイル数の増加を抑えている。対象は一覧から
  辿れる範囲（`publish.history_limit` 件）に限定する。

### 修正

- 古いページの掃除処理が `matches/` を絞り込みディレクトリと誤認して削除していた。
  予約ディレクトリ（`cards` / `matches`）を除外対象に加えた。

---

## [1.3.1] - 2026-09-07

### 削除

- デッキ名の手動設定を廃止した。閲覧者（友人）が操作できない非対称な機能のため。
  デッキの呼び名はコストの高いカード3枚から自動生成する方式に変更した。

---

## [1.3.0] - 2026-09-07

### 変更

- **公開サイトのみでの運用を標準とした。** ローカルのダッシュボードは起動不要になった。
  ただしFlaskアプリのコードは静的サイト生成に必要なため削除していない。
- 公開サイトの履歴件数を設定可能にした（`publish.history_limit`）。

### リファクタリング

- 集計の定型句を `stats.from_row()` へ集約した（5箇所 → 1箇所）。
- Flaskの6画面を `@screen` デコレータへ集約した。
- 画面定義を `urls.SCREENS` に一元化し、ルートパスの二重管理を解消した。

### 追加

- 回帰確認の仕組み（`tests/check_baseline.py`）。12通りの絞り込みの集計値を
  基準値と照合し、意図しない数値の変化を検出する。

### 修正

- **デッキグループIDが収集のたびに変わっていた問題。** 絞り込みURLが毎日変わり、
  公開サイトに不要なページが蓄積していた。代表デッキが同じならIDを引き継ぐよう変更し、
  書き出し時に不要ページを削除するようにした。

---

## [1.2.0] - 2026-09-07

### 追加

- **静的サイトとしての公開**（`src/exporter/`）。絞り込みの全組み合わせをあらかじめ
  HTMLとして書き出すため、閲覧にサーバーが要らない。推測困難な32文字のディレクトリ名に
  より、URLを知る人だけが閲覧できる。Cloudflare Pages へのアップロードまで
  `src.exporter.publish` で一括実行する。
- **PCがスリープ中でも閲覧できるようになった。** 更新はPCが起動して収集したときに行われる。
- カード画像の最適化。285px PNG から 132px WebP へ変換し、1画面あたりの通信量が
  13.2MB から 0.34MB へ減った。
- テンプレートの共用。サーバー稼働時と静的サイトでURL生成のみを切り替え、画面定義を
  1つに保つ仕組み（`src/web/urls.py`）。

---

## [1.1.0] - 2026-09-06

### 追加

- **複数プレイヤーの追跡**（`players` テーブル、収集のプレイヤー別ループ、画面上部の切替）。
  デッキとデッキグループもプレイヤー単位に分離し、集計は常に1人に閉じる。友人のトークンは
  不要で、対戦履歴は公開情報のためタグだけで取得できる。スキーマ変更は
  `src/db/migrations.py` で既存データを保ったまま移行する。
- スマートフォン対応（640px以下のレイアウト調整）。
- PWA対応。ホーム画面に追加するとアプリのように起動する。

### 変更

- 既定ポートを 8730 へ変更し、ポート衝突を検知するようにした。5000番は他アプリと
  衝突しやすく、Windowsでは二重バインドが起きて別アプリの画面が表示される事故があった。

---

## [1.0.0] - 2026-09-05

### 追加

- APIクライアント（RoyaleAPIプロキシ対応・リトライ・エラー処理）。
- SQLiteのスキーマとリポジトリ層。
- 収集スクリプトとWindowsタスクスケジューラへの登録。
- カードマスタの取得と日本語名の対応表。
- 分析: 全体勝率 / 期間別勝率 / 相手カード別勝率 / 自デッキ別勝率 / トロフィー推移 /
  レベル差分析。
- Wilson信頼区間と最低試合数によるグレー表示。
- デッキ類似度によるグループ化。
- Flaskダッシュボード（画面1〜6）。
- 診断コマンド・バックアップコマンド。
